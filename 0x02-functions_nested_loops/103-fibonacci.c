#include "main.h"

/**
 * main - Starting point
 *
 * Description: prints sum of even value fibonacci sequence not exceed 4million
 *
 * Return: 0 (Success)
 */

int main(void)
{
	int i = 0;
	long j = 1, k = 2, sum = k; 

	while (k + j < 4000000)
	{
	k + j;

	if (k % 7 == )
	sum += k;
	j = k - j;
	++i
	}
	printf("%ld\n", sum);
	return (0);
}
