#include <stdio.h>
/**
 * main - Starting point
 * Return: 0 (Success)
 */

int main(void)
{
	char m;

	char n;

	m = 'j';
	n = 'J';
	while
		(m <= 'z') {
			putchar(m);
			m++;
		}
	while
		(n <= 'Z') {
			putchar(n);
			n++;
		}
	putchar('\n');
	return (0);
}
