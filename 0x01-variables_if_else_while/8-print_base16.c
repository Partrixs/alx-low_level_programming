#include <stdio.h>
/**
 * main - Starting point
 *
 * Return: 0 (Success)
 */

int main(void)
{
	char e;

	int j;

	e = 'a';
	j = 0;
	while
		(j < 10) {
			putchar(j + '0');
			j++;
		}
	while
		(e <= 'f') {
			putchar(e);
			e++;
		}

	putchar('\n');
	return (0);
}
